name: demo_soma_offline
description: SOMA Offline Demo - Buyer & Seller
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.27.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1
  intl: ^0.19.0
  qr_flutter: ^4.1.0   # ← نسخه سازگار و پایدار

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

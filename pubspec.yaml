name: soma_app_qb
description: "SOMA Offline – Buyer & Seller (BLE + QR)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # BLE (tested with Flutter 3.19.x / AGP 7.4.2)
  flutter_blue_plus: ^1.31.15

  # Local storage for balances & receipts
  shared_preferences: ^2.2.3

  # Runtime permissions (Android 12+ BLE + Location)
  permission_handler: ^11.3.1

  # Formatting & QR (optional but enabled)
  intl: ^0.19.0
  qr_flutter: ^4.2.3

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
```0

// Top-level build for Gradle 8 / AGP 8
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    // (بدون classpath قدیمی — از plugins DSL استفاده می‌کنیم)
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
